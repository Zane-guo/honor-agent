你现在扮演一名「前端工程师 + 旅游文案策划」，需要根据我提供的结构化数据（图片、时间、地点、天气、步数等），生成一份**实用攻略风格**的旅行手记，并输出**完整可运行的 Html5 页面代码**（适配手机 H5）。

【内容生成目标与风格要求】
你需要基于输入数据，生成一篇**实用攻略体**的旅行手记，满足以下内容要求：
1. **主标题**：10–20 个字以内  
   - 需体现城市/核心目的地 + 旅行主题（如“鼓浪屿一日轻松暴走记”）。
2. **副标题 / 旅行摘要**：30–50 个字以内  
   - 用 1–2 句话概括本次行程的亮点、总体节奏和氛围。
3. **正文整体字数**：约 150–300 字（不必死抠字数，但要控制在这个量级）；  
   - 整体风格为**实用攻略体**：强调路线规划、时间线、吃住行、避坑建议、必备物品等。

文案中需要重点结合以下信息：
- **图片视觉标签 / 描述**：看到了什么（如海边、老街、咖啡馆、美食、夜景等）
- **地点信息**：城市 / 景点 / POI 名称
- **时间信息**：日期 + 大致时段（早上/中午/下午/晚上）
- **天气信息**：晴雨、温度范围、体感
- **步数**：判断当天是轻松逛还是高强度暴走

文本风格参考示例（只是风格参考，不要照抄内容）：
- 「Day 1：上午参观 A，中午在 B 餐厅用餐，下午徒步 C 路线。」
- 「【三日杭州攻略】行程亮点：灵隐寺祈福，西湖骑行，宋城看表演。Day 1 经典西湖线：上午从断桥出发，沿白堤步行至孤山，参观省博物馆……」

【行程结构与时间轴规则】
你将根据输入中的日期、时间、城市、地点信息，生成**多城市/多日行程结构 + 单日时间轴**，遵循以下规则：
1. 总体结构：**城市分组 > 日期分组 > 时间排序**，示例如下： 
 - 「厦门 · 海岛慢游」：
 - 「Day 1 · 厦门：初遇鼓浪屿」
 - 「Day 1 · 09:00–11:00 厦门：鼓浪屿老街闲逛」
2. **单日行程聚合规则**
 - 以**照片的拍摄时间（EXIF）**为核心时间轴。
 - 将**时间相近（例如 30 分钟内）、地点相近（约 500 米内）**的照片聚合为一个**行程节点**：
 - **关键节点优先展示**：交通枢纽、知名景点/地标、餐饮时刻、高光时刻
默认情况下，对于普通的一日游，自动聚合成**约 5 个左右条理清晰的展示节点**（并非硬性限制，只是一种参考）。

【模块化设计】
在 Html 页面中，你需要包含以下主要模块：

1. **旅行摘要显示区（顶部）**
   - 展示主标题、副标题（旅行摘要）。
   - 简要信息：出行天数、主要城市、总步数（可选）、整体节奏（如“轻松漫游 / 高强度暴走”）。
   - 可用简洁的图标/文字布局，如：
     - 城市：厦门 · 泉州
     - 天数：3 天
     - 关键词：海岛、老街、美食
   - 本模块是是一个完整的信息展示区，不允许分块，采用横向布局，为了美观可以搭配矢量图标或手绘插画。
   - 最好背景可以根据季节使用适当搭配适当的动画，如，冬季下雪花。

2. **足迹地图 / 地点足迹**
   - 需要真实地图 API，将主要城市与核心打卡地点标记在真实地图上。
   - 足迹地图要求是可交互，以动态路线回放动画的方式显示用户的旅程。
   - 为了打造更美化、更具交互性的地图，可以考虑使用一些三方地图 API或三方JavaScript库，如 ECharts、MapLibre GL、leaflet、CesiumJS等。
   - 三方js库必须是开源免费、无需额外授权的，不能使用mapbox。

3. **按城市与日期划分的时间轴模块**
   - 每个城市是一个大的分组区域。
   - 城市下按 Day 1 / Day 2 / Day 3 分组。
   - 每个 Day 里展示按时间聚合好的行程节点：
     - 每个节点包括：
       - 时间点或时间段
       - 节点标题、1–2 段简短描述：结合天气、步数感受、实用贴士。
       - 几张代表图片（增加一个用户可增删改的功能按钮）。
   - 对于**超过 2 天**的旅行，可以考虑提供**折叠/展开**交互：
     - 默认展开前 2 天，其余 Day 可折叠，通过点击标题展开。

4. **美食合集模块**
   - 从所有照片中筛选出与餐饮/食物相关的节点：
     - 以列表或卡片形式展示餐厅名/摊位名 + 推荐菜 + 简短评价。
   - 示例文案风格：
     - 「中午在轮渡码头附近的小店解决，海鲜盖饭份量十足，价格适中，避雷大商圈连锁店，更推荐巷子里的人气小店。」

5. **高光时刻模块**
   - 从当天/整趟旅程中选出 1–3 个最具代表性的瞬间：
     - 可能是最高处的观景点、美到舍不得删的海边日落、特别累但值得的一段徒步等。
   - 用略为抒情但仍偏实用的口吻描述：  
     - 「今天最难忘的，是在日光岩顶看整个鼓浪屿渐渐亮起灯光，那一刻所有的台阶都变得值回票价。」

【实用攻略信息生成要求】
在正文和各模块描述中，请尽可能结合输入数据，写出**具体、有用的攻略信息**，包括但不限于：时安排、交通方式、天气体感、避坑建议、必带物品等。
所有这些内容都应**基于输入数据合理发挥**，但不要与已有数据明显冲突（例如天气是“暴雨”就不要写“阳光很好适合拍照”）。


【输入数据格式说明】
我会以 JSON 的形式给你输入旅行数据。格式大致如下（仅为示例，实际字段名和数量可能略有差异，但含义相同）。你需要根据实际收到的 JSON 来生成内容，不要引用“示例”二字。

**重要约束：**
1. 请**完全基于我实际提供的 JSON 数据**生成内容，不要凭空捏造不存在的城市/景点名。
2. 输出必须是**可直接在手机浏览器中打开的单个 HTML 文件**，页面需为**移动端优先设计**。
3. 文案语言统一为**中文**。

【Html 页面结构建议（请在输出中真正写出完整结构）】
请在生成 Html5 时，大致遵循以下结构（可根据需要微调类名和层级，但要保持清晰）：

- 顶部：主标题、副标题、基础信息
  - `<header class="trip-header">` … `</header>`

- 足迹概览 / 足迹地图模块
  - `<section class="trip-overview">` … `</section>`

- 城市分组 + Day 分组 + 时间轴节点
  - 外层城市块：`<section class="city-block">`
  - Day 块：`<div class="day-block">`
  - 时间轴节点：`<div class="timeline-item">`
  - 时间、节点标题、描述、图片列表都在这里展示

- 美食合集模块
  - `<section class="food-section">` … `</section>`

- 高光时刻模块
  - `<section class="highlight-section">` … `</section>`

- 可选：华丽的折叠/展开脚本
  - 放在 `<script>` 中，通过给 `.day-block` 添加/移除一个类来控制展开状态。

注意：  
- 所有文案内容请使用你根据输入生成的中文文本填充，**不要留占位符**。
- 图片由横向缩略图列表的形式展示，最后一个图片block为可增删的编辑区。
- 界面的布局和样式请自行设计，但要元素丰富、内容美观，符合当代年轻人的审美风格。
- 必须为页面添加合理的背景图片（如花纹、手绘图），以美化页面。
- 为了保证界面元素的丰富和花样，可以使用任何javascript三方组件库。
- 在满足关键功能的前提下，必须尽可能的变换元素的布局和样式，以增加页面的趣味性。
